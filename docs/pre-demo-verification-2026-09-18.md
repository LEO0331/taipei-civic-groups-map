# Pre-demo verification — 2026-09-18

This document records the pre-demo verification baseline for the merged A–H remediation and polish work.

## Production baseline

- Verified branch: `main`
- Verified commit: `9031db0c3b75eeae7d0bb756b0bd9aee78bf3cb5`
- Merge: PR #9, `polish/h-ui-family-normalization`
- GitHub Pages workflow run: `35298325891`
- Deployment result: successful
- Production URL: https://leo0331.github.io/taipei-civic-groups-map/

The deployment workflow fetched current source data, converted it, ran type checking, unit tests and Playwright, built the Vite production bundle, uploaded release evidence, and deployed the exact verified commit to GitHub Pages.

## Verification results

| Check | Result |
| --- | --- |
| `npm run data:fetch` | Passed |
| `npm run data:convert` | Passed |
| `npm run typecheck` | Passed |
| `npm test` | Passed |
| Playwright desktop project | Passed |
| Playwright mobile project | Passed |
| Production build | Passed |
| Data release evidence upload | Passed |
| GitHub Pages deploy | Passed |
| Playwright total | 91 passed, 1 expected desktop-only skip, 0 failed |

The single skip is intentional: the representative mobile-overflow test runs only in the mobile project; its mobile execution passed.

## Release-data evidence

The release evidence generated by this deployment reports:

| Metric | Value |
| --- | ---: |
| Static dataset directories tracked by Data Trust | 117 |
| Directories with a readable source date | 32 |
| Directories with unknown source date | 85 |
| Refresh fallbacks / reused snapshots | 0 |

Unknown source dates are surfaced as a caution state. A reused snapshot would be surfaced as a stronger warning. This deployment did not need a reused-snapshot fallback.

## User workflows verified

The automated browser suite verifies the following on both desktop and mobile where applicable:

- every catalogued module opens without a runtime error;
- catalogue discovery and representative search/filter interactions;
- direct dataset URLs;
- dataset selection scrolling to the dataset content rather than the masthead;
- Back/Forward restoration of dataset and language state;
- shareable and persistent language selection;
- persistent onboarding dismissal;
- safe recovery from an invalid dataset URL;
- keyboard tab semantics and Arrow/Home/End navigation;
- readable loading, empty, malformed-data and request-failure states;
- isolation of unrelated background-data failures;
- lazy loading of unrelated modules;
- representative mobile overflow protection;
- Data Trust hierarchy and localized dataset labels;
- the six UI-family classification contract.

## Location-distribution correctness

The pre-polish geography correction remains covered by dedicated E2E tests:

- 安寧緩和醫療機構 uses a city/county distribution rather than an empty district distribution;
- administrative codes such as `63000000` are presented as readable names such as 臺北市;
- 外縣市殯葬業者 aggregates the source city/county field;
- generated directories with no usable geographic field omit the misleading distribution view;
- datasets with a real district dimension keep the 行政區分布 contract.

## UI-family baseline

All 156 catalogue routes/views resolve to one of six UI families:

| UI family | Classified routes/views |
| --- | ---: |
| `healthcare-rich-directory` | 2 |
| `healthcare-standard` | 56 |
| `location-directory` | 13 |
| `registry-directory` | 57 |
| `records-analysis` | 12 |
| `statistics-analysis` | 16 |

Batch H directly migrated representative legacy modules onto the shared `DatasetFamilyFrame`, `DatasetFamilyHeading`, and native accessible tab primitives:

- registry directory: Performing Arts Groups, Registered Labor Unions, Licensed Pawnshops;
- records analysis: Labor Standards Act Violation Records;
- statistics analysis: Alternative Service Reserve Statistics.

Remaining legacy modules can migrate family-by-family after the demo without changing their domain/data logic.

## Non-blocking items

No correctness, navigation, accessibility, data-refresh, build, or deployment blocker was found for the demo.

Known non-blocking engineering items:

1. The main production JavaScript chunk is approximately **549.5 kB minified / 161.4 kB gzip**, so Vite still emits its >500 kB chunk warning. Dataset modules are nevertheless route-level lazy chunks.
2. GitHub Actions emits runtime/deprecation notices for older action internals, including Node 20-targeting action metadata being executed on newer GitHub-hosted runners.
3. Source-date coverage is incomplete: 85 of 117 Data Trust directories currently have no readable source date.
4. Full legacy-shell migration to the six UI families is intentionally post-demo work.

## Final manual demo spot-check

Before presenting, a short human browser pass should cover:

1. 人民團體 — open the catalogue, change views, search/filter.
2. 安寧緩和醫療機構 — verify 縣市分布 renders readable city/county data.
3. 成人流感疫苗合約醫療院所 — verify rich healthcare layout and Data Trust caution state.
4. 臺北市物理治療所 — verify location-directory flow and keyboard tabs.
5. 工會名單 — verify normalized registry-directory shell.
6. 勞基法違規公布紀錄 — verify normalized records-analysis shell.
7. 替代役備役列管人數分析統計 — verify normalized statistics-analysis shell.
8. Change language, reload, then use Back/Forward on a dataset route.

## Release posture

Feature work should remain frozen until after the demo. Any issue found in the final manual spot-check should be handled as a narrow pre-demo bug fix rather than another broad refactor.

Post-demo cleanup should prioritize:

1. remaining `registry-directory` legacy shells;
2. remaining `records-analysis` shells;
3. remaining `statistics-analysis` shells;
4. remaining `location-directory` shells;
5. exceptional healthcare modules;
6. main-bundle reduction and CI-action maintenance.
