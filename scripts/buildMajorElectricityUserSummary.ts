import { readFile,writeFile } from 'node:fs/promises';import { join } from 'node:path';import { buildMajorElectricityUserSummary } from '../src/lib/majorElectricityUsers';import type { MajorElectricityUserRecord } from '../src/types';const dir=join(process.cwd(),'public/data/major-electricity-users'),records=JSON.parse(await readFile(join(dir,'records.json'),'utf8')) as MajorElectricityUserRecord[];await writeFile(join(dir,'summary.json'),JSON.stringify(buildMajorElectricityUserSummary(records)));console.log(`Built major electricity user summary for ${records.length} records.`);
