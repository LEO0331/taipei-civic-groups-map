import { readFile, writeFile } from 'node:fs/promises'; import { join } from 'node:path'; import { buildPublicPneumococcalVaccineProviderSummary } from '../src/lib/publicPneumococcalVaccineProviders'; import type { PublicPneumococcalVaccineMedicalProviderRecord } from '../src/types'; const dir = join(process.cwd(), 'public/data/public-pneumococcal-vaccine-providers'); const records = JSON.parse(await readFile(join(dir, 'records.json'), 'utf8')) as PublicPneumococcalVaccineMedicalProviderRecord[]; await writeFile(join(dir, 'summary.json'), JSON.stringify(buildPublicPneumococcalVaccineProviderSummary(records))); console.log(`Built public pneumococcal vaccine provider summary for ${records.length} records.`);
