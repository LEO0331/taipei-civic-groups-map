import {readFile,writeFile}from'node:fs/promises';import{join}from'node:path';import{buildRegisteredPostpartumCareInstitutionSummary}from'../src/lib/registeredPostpartumCareInstitutions';import type{RegisteredPostpartumCareInstitutionRecord as R}from'../src/types';const d=join(process.cwd(),'public/data/registered-postpartum-care-institutions'),r=JSON.parse(await readFile(join(d,'records.json'),'utf8')) as R[];await writeFile(join(d,'summary.json'),JSON.stringify(buildRegisteredPostpartumCareInstitutionSummary(r)));console.log(`Built postpartum-care institution summary for ${r.length} records.`);
