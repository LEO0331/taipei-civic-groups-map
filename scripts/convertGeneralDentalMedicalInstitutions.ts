import{createHash}from'node:crypto';import{mkdir,readFile,writeFile}from'node:fs/promises';import{join}from'node:path';import{parseCsv}from'./convertCivicGroups';import{clean,district,norm,road,summary}from'../src/lib/generalDentalMedicalInstitutions';const raw=join(process.cwd(),'data/raw/general-dental-medical-institutions/general-dental-medical-institutions.csv'),out=join(process.cwd(),'public/data/general-dental-medical-institutions'),rp=join(process.cwd(),'public/data/conversion-report.json'),b=await readFile(raw);let t:string;try{t=new TextDecoder('utf-8',{fatal:true}).decode(b).replace(/^\uFEFF/,'')}catch{t=new TextDecoder('big5').decode(b)}const[h,...rows]=parseCsv(t),heads=h.map(x=>clean(x)??''),seen=new Set<string>(),dupes:string[]=[],missing:string[]=[],unknown:string[]=[];const r=rows.flatMap((row,i)=>{const v=Object.fromEntries(heads.map((x,j)=>[x,clean(row[j])])),sourceSequenceNumber=v['序號'],institutionName=v['機構名稱'],postalCode=v['郵遞區號'],address=v['地址'],phone=v['電話'];if(!institutionName||!address){missing.push(String(i+2));return[]}const k=`${norm(institutionName)}|${norm(address)}|${norm(phone)}`;if(seen.has(k)){dupes.push(k);return[]}seen.add(k);const districtNameFromAddress=district(address);if(!districtNameFromAddress)unknown.push(String(i+2));return[{id:createHash('sha1').update(k).digest('hex').slice(0,12),module:'general_dental_medical_institutions',sourceSequenceNumber,institutionName,institutionNameNormalized:norm(institutionName),postalCode,postalCodeNormalized:clean(postalCode),districtNameFromAddress,address,addressNormalized:norm(address),roadName:road(address),phone,phoneNormalized:phone?.replace(/[\s（）()－–—]/g,''),hasPhone:Boolean(phone),googleMapsQuery:clean(`${address} ${institutionName}`),source:'臺北市牙醫一般科醫療機構',sourceAgency:'臺北市政府衛生局'}]});const s=summary(r),report=await readFile(rp,'utf8').then(JSON.parse).catch(()=>({}));await mkdir(out,{recursive:true});await Promise.all([writeFile(join(out,'records.json'),JSON.stringify(r)),writeFile(join(out,'summary.json'),JSON.stringify(s)),writeFile(rp,JSON.stringify({...report,generalDentalMedicalInstitutions:{convertedAt:new Date().toISOString(),headers:heads,outputRecords:r.length,duplicates:dupes,missingFields:missing,unknownDistricts:unknown,notes:['No official coordinates supplied; no exact markers or geocoding created.']}},null,2))]);console.log(`Converted ${r.length} general dental institutions.`);
