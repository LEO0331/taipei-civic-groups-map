import{createHash}from'node:crypto';import{mkdir,readFile,writeFile}from'node:fs/promises';import{join}from'node:path';import{parseCsv}from'./convertCivicGroups';const b=process.cwd(),o=join(b,'public/data/healthcare-welfare-budget'),raw=await readFile(join(b,'data/raw/healthcare-welfare-budget/source.csv'));let s=new TextDecoder().decode(raw).replace(/^\uFEFF/,'');if(!s.includes('原預算數'))s=new TextDecoder('big5').decode(raw).replace(/^\uFEFF/,'');const[h,...rows]=parseCsv(s),hs=h.map(x=>x.trim()),n=(v?:string)=>{const x=(v??'').replace(/[,\s]/g,'');return /^-?\d+(\.\d+)?$/.test(x)?Number(x):null},r=rows.map(x=>{const sourceValues=Object.fromEntries(hs.map((k,j)=>[k,(x[j]??'').trim()])),originalBudgetRaw=sourceValues['原預算數']??'',budgetChangeRaw=sourceValues['預算增減數']??'',totalBudgetRaw=sourceValues['合計數']??'';return{id:createHash('sha1').update(JSON.stringify(sourceValues)).digest('hex').slice(0,16),item:sourceValues['項次']??'',name:sourceValues['名稱']??'',originalBudgetRaw,budgetChangeRaw,totalBudgetRaw,originalBudget:n(originalBudgetRaw),budgetChange:n(budgetChangeRaw),totalBudget:n(totalBudgetRaw),sourceValues}});await mkdir(o,{recursive:true});await writeFile(join(o,'records.json'),JSON.stringify(r));
