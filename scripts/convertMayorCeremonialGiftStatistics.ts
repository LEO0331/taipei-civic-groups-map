import{createHash}from'node:crypto';import{mkdir,readFile,writeFile}from'node:fs/promises';import{join}from'node:path';import{parseCsv}from'./convertCivicGroups';const b=process.cwd(),o=join(b,'public/data/mayor-ceremonial-gift-statistics'),s=new TextDecoder().decode(await readFile(join(b,'data/raw/mayor-ceremonial-gift-statistics/source.csv'))).replace(/^\uFEFF/,''),[h,...rows]=parseCsv(s),hs=h.map(x=>x.trim()),n=(v?:string)=>{const x=(v??'').replace(/[\s,]/g,'');return /^\d+$/.test(x)?Number(x):null},r=rows.map(x=>{const sourceValues=Object.fromEntries(hs.map((k,j)=>[k,(x[j]??'').trim()])),year=Number(sourceValues['年度']),month=Number(sourceValues['月份']),scroll=n(sourceValues['中堂數量']),screen=n(sourceValues['鏡屏數量']),card=n(sourceValues['結婚賀卡數量']),elegiac=n(sourceValues['電子輓聯數量']),letter=n(sourceValues['慰問函數量']);return{id:createHash('sha1').update(JSON.stringify(sourceValues)).digest('hex').slice(0,16),year,month,scroll,screen,card,elegiac,letter,total:[scroll,screen,card,elegiac,letter].reduce<number>((a,v)=>a+(v??0),0),sourceValues}});await mkdir(o,{recursive:true});await writeFile(join(o,'records.json'),JSON.stringify(r));
