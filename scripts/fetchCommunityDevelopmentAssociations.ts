import { mkdir, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
const datasetId = 'e81a07f7-8137-4019-a96f-210a00ef72a3', sourcePage = `https://data.taipei/dataset/detail?id=${datasetId}`, rawDirectory = join(process.cwd(), 'data/raw/community-development-associations');
await mkdir(rawDirectory, { recursive: true }); const page = (await (await fetch(sourcePage)).text()).replaceAll('\\u0026', '&'); const sourceFileUpdatedAt = page.match(/data-th="更新時間"[^>]*>\s*<span[^>]*>\s*([^<\s][^<]*?)\s*<\/span>/)?.[1] ?? '', metadataUpdatedAt = page.match(/詮釋資料更新時間[\s\S]{0,240}?<td[^>]*>\s*([^<\s][^<]*?)\s*<\/td>/)?.[1] ?? '', path = page.match(new RegExp(`/api/dataset/${datasetId}/resource/[0-9a-f-]{36}/download[^"'<>]*`, 'i'))?.[0];
if (!path) throw new Error('The official dataset page did not expose a downloadable resource.'); const sourceUrl = `https://data.taipei${path}`, response = await fetch(sourceUrl); if (!response.ok) throw new Error(`Official CSV download failed: ${response.status} ${response.statusText}`); const bytes = Buffer.from(await response.arrayBuffer()); await Promise.all([writeFile(join(rawDirectory, 'source.csv'), bytes), writeFile(join(rawDirectory, 'fetch-metadata.json'), JSON.stringify({ datasetId, sourcePage, sourceUrl, sourceFileUpdatedAt, metadataUpdatedAt, downloadedAt: new Date().toISOString(), fileSize: bytes.length }, null, 2))]); console.log('Fetched community development associations CSV.');
