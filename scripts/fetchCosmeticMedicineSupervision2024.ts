import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path'; const response = await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=c6e9d1a8-61d4-4f64-a965-b4d08e6e7050'); if (!response.ok) throw new Error(`Official CSV request failed: ${response.status}`); const dir = join(process.cwd(), 'data/raw/cosmetic-medicine-supervision-2024'); await mkdir(dir, { recursive: true }); await writeFile(join(dir, 'source.csv'), Buffer.from(await response.arrayBuffer())); console.log('Fetched cosmetic medicine supervision CSV.');
