import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path'; const response = await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=1a6a6056-4649-48be-b071-fba88d025d10'); if (!response.ok) throw new Error(`Official CSV request failed: ${response.status}`); const dir = join(process.cwd(), 'data/raw/family-medicine-institutions'); await mkdir(dir, { recursive: true }); await writeFile(join(dir, 'source.csv'), Buffer.from(await response.arrayBuffer())); console.log('Fetched family medicine institution CSV.');
