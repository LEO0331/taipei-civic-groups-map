import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path';
const sourcePage = 'https://data.taipei/dataset/detail?id=a7462972-0380-4987-99fb-2071e798ce66'; const resourceUrl = 'https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=57685fb2-cc92-4874-8588-e5a1b1b15b50'; const dir = join(process.cwd(), 'data/raw/fixed-site-temporary-childcare');
const response = await fetch(resourceUrl); if (!response.ok) throw new Error(`Download failed: ${response.status}`); const bytes = new Uint8Array(await response.arrayBuffer()); await mkdir(dir, { recursive: true }); await Promise.all([writeFile(join(dir, 'source.csv'), bytes), writeFile(join(dir, 'fetch-metadata.json'), JSON.stringify({ sourcePage, resourceUrl, sourceAgency: '臺北市政府社會局', sourceFileUpdatedAt: '2025-06-13T18:36:01+08:00', metadataUpdatedAt: '2025-06-13T18:36:50+08:00', updateFrequency: 'annual', fetchedAt: new Date().toISOString() }, null, 2))]); console.log(`Fetched ${bytes.length} bytes of fixed-site temporary childcare records.`);
