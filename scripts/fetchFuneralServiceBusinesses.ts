import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path'; const sourcePage = 'https://data.taipei/dataset/detail?id=20143c69-a790-4dec-bb5e-1f0ed7e76c1e'; const sourceUrl = 'https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=702cd8d7-3fa7-43df-a6fa-7d084221dc2e'; const output = join(process.cwd(), 'data/raw/funeral-service-businesses'); await mkdir(output, { recursive: true }); const response = await fetch(sourceUrl); if (!response.ok) throw Error(`Download failed: ${response.status}`); const data = Buffer.from(await response.arrayBuffer()); await Promise.all([writeFile(join(output, 'source.csv'), data), writeFile(join(output, 'fetch-metadata.json'), JSON.stringify({ sourcePage, sourceUrl, downloadedAt: new Date().toISOString(), fileSize: data.length, sourceAgency: '臺北市殯葬管理處', sourceFileUpdatedAt: '2025-06-11T10:33:01+08:00', metadataUpdatedAt: '2025-06-11T10:33:18+08:00', updateFrequency: 'annual' }, null, 2))]); console.log(`Fetched ${data.length} bytes of funeral service business records.`);
