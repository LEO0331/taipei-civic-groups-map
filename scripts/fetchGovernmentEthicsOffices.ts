import{mkdir,writeFile}from'node:fs/promises';import{join}from'node:path';const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=84f438d7-7e9e-4d7a-af5e-3713712d3ea7');if(!r.ok)throw Error(String(r.status));const d=join(process.cwd(),'data/raw/government-ethics-offices');await mkdir(d,{recursive:true});await writeFile(join(d,'source.csv'),new Uint8Array(await r.arrayBuffer()));console.log('Fetched ethics office source.');
