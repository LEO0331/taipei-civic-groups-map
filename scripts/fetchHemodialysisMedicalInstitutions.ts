import{mkdir,writeFile}from'node:fs/promises';import{join}from'node:path';const id='45674f0f-0a02-4161-aa9f-377917ba16da',dir=join(process.cwd(),'data/raw/hemodialysis-medical-institutions'),page=`https://data.taipei/dataset/detail?id=${id}`;await mkdir(dir,{recursive:true});const t=await(await fetch(page)).text(),rel=t.match(new RegExp(`/api/dataset/${id}/resource/[0-9a-f-]{36}/download`,'i'))?.[0];if(!rel)throw Error('No CSV resource');const url=new URL(rel,'https://data.taipei').toString(),r=await fetch(url),b=Buffer.from(await r.arrayBuffer());await Promise.all([writeFile(join(dir,'source.csv'),b),writeFile(join(dir,'fetch-metadata.json'),JSON.stringify({sourcePage:page,sourceUrl:url,downloadedAt:new Date().toISOString(),fileSize:b.length},null,2))]);console.log(`Fetched ${b.length} bytes.`);
