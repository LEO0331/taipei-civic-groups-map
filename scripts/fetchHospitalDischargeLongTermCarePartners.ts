import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path';
const sourcePage = 'https://data.taipei/dataset/detail?id=05efb8d6-fa6b-494a-af3a-d26a31ccfe88'; const sourceUrl = 'https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=25d1fe89-a4c9-47d1-bbb5-ad5b26fd4771'; const output = join(process.cwd(), 'data/raw/hospital-discharge-long-term-care-partners'); await mkdir(output, { recursive: true }); const response = await fetch(sourceUrl); if (!response.ok) throw new Error(`Download failed: ${response.status}`); const data = Buffer.from(await response.arrayBuffer()); await Promise.all([writeFile(join(output, 'source.csv'), data), writeFile(join(output, 'fetch-metadata.json'), JSON.stringify({ sourcePage, sourceUrl, downloadedAt: new Date().toISOString(), fileSize: data.length, sourceAgency: '臺北市政府衛生局', sourceFileUpdatedAt: '2026-06-30T15:21:37+08:00', metadataUpdatedAt: '2026-07-13T16:49:13+08:00', updateFrequency: 'irregular' }, null, 2))]); console.log(`Fetched ${data.length} bytes of hospital discharge-to-LTC partner records.`);
