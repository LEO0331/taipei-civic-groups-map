import{mkdir,writeFile}from'node:fs/promises';import{join}from'node:path';const raw=join(process.cwd(),'data/raw/out-of-city-funeral-service-businesses'),page='https://data.taipei/dataset/detail?id=f0b36723-4813-4664-aeda-4176e2576f52';await mkdir(raw,{recursive:true});const h=(await(await fetch(page)).text()).replaceAll('\\u0026','&'),p=h.match(/\/api\/dataset\/f0b36723-4813-4664-aeda-4176e2576f52\/resource\/[0-9a-f-]{36}\/download[^"'<>]*/i)?.[0];if(!p)throw Error('No CSV resource found');const url=`https://data.taipei${p}`,res=await fetch(url);if(!res.ok)throw Error(`Download failed: ${res.status}`);const b=Buffer.from(await res.arrayBuffer());await writeFile(join(raw,'out-of-city-funeral-service-businesses.csv'),b);await writeFile(join(raw,'fetch-metadata.json'),JSON.stringify({sourcePage:page,sourceUrl:url,downloadedAt:new Date().toISOString(),fileSize:b.length},null,2));console.log('Prepared out-of-city funeral business CSV.');
