import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path';
const response = await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=d23ab545-133c-43d9-ad47-8147ab5f3289'); if (!response.ok) throw new Error(`Official CSV request failed: ${response.status}`); const dir = join(process.cwd(), 'data/raw/registered-after-school-care-centres'); await mkdir(dir, { recursive: true }); await writeFile(join(dir, 'source.csv'), Buffer.from(await response.arrayBuffer())); console.log('Fetched registered after-school care centre CSV.');
