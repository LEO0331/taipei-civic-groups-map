import { mkdir, writeFile } from 'node:fs/promises'; import { join } from 'node:path'; const response = await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=380592d1-bc02-4268-896d-25fc7571e549'); if (!response.ok) throw new Error(`Official CSV request failed: ${response.status}`); const dir = join(process.cwd(), 'data/raw/subsidized-senior-residential-placement-institutions'); await mkdir(dir, { recursive: true }); await writeFile(join(dir, 'source.csv'), Buffer.from(await response.arrayBuffer())); console.log('Fetched subsidized senior residential placement institution CSV.');
