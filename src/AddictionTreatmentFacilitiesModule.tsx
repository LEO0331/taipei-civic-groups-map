import{useEffect,useState}from'react';export default function AddictionTreatmentFacilitiesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/addiction-treatment-facilities/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'藥癮戒治機構':'Addiction Treatment Facilities'}</h1><p className="notice subtle">{language==='zh'?'服務內容及門診資訊可能變更，就醫前建議先向機構確認。':'Services and appointment information may change; confirm directly with the institution before visiting.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、地區、地址、模式或服務':'Search institution, area, address, mode, or service'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.city,x.address,x.treatmentMode,x.servicesRaw].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.city} · {x.treatmentMode}</p><p>{x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p><details><summary>{language==='zh'?'戒治服務':'Treatment services'}</summary><p>{x.servicesRaw}</p></details><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
