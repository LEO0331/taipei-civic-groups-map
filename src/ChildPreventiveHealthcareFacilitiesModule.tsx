import{useEffect,useState}from'react';export default function ChildPreventiveHealthcareFacilitiesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/child-preventive-healthcare-facilities/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'兒童預防保健醫療院所':'Child Preventive Healthcare Facilities'}</h1><p className="notice subtle">{language==='zh'?'僅限來源列示的兒童預防保健醫療院所；實際服務與安排請直接向院所確認。':'Only source-listed child preventive healthcare facilities are shown. Confirm actual services and arrangements directly with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋院所、代碼、行政區或地址':'Search facility, code, district, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.facilityCode,x.district,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p><p>{language==='zh'?'醫事機構代碼':'Facility code'}: {x.facilityCode||'—'}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
