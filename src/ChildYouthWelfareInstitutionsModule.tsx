import{useEffect,useState}from'react';export default function ChildYouthWelfareInstitutionsModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/child-youth-welfare-institutions/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'兒少福利機構':'Child & Youth Welfare Services'}</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、類型或地址':'Search institution, type, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.institutionType,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.institutionType}</p><p>{x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}{x.email&&<> · <a href={`mailto:${x.email}`}>{language==='zh'?'寄送 Email':'Email'}</a></>}</p></article>)}</div></section>}
