import{useEffect,useState}from'react';export default function ClinicalPathologyFacilitiesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/clinical-pathology-facilities/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'臨床病理科醫療機構':'Clinical Pathology Facilities'}</h1><p className="notice subtle">{language==='zh'?'本目錄僅呈現來源列示的臨床病理科醫療機構；請直接向機構確認實際服務。':'This directory presents source-listed clinical pathology facilities only. Confirm actual services directly with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區、郵遞區號或地址':'Search institution, district, postal code, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.district,x.postalCode,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.postalCode||'—'}</p><p>{x.address}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
