import{useEffect,useState}from'react';export default function DisabilityDayServicesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/disability-day-services/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'身心障礙日間服務機構':'Disability Day Service Institutions'}</h1><p className="notice subtle">{language==='zh'?'本目錄僅呈現來源機構聯絡資料；請直接向機構確認實際服務與安排。':'This directory presents source institution contacts only; confirm actual services and arrangements with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區或地址':'Search institution, district, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.district,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.address||'—'}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p>{x.fax&&<p>Fax: {x.fax}</p>}{x.address&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a>}</article>)}</div></section>}
