import{useEffect,useState}from'react';export default function EarlyInterventionCommunityServicesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/early-intervention-community-services/records.json`).then(x=>x.json()).then(setR)},[]);const f=r.filter(x=>!q||[x.name,x.district,x.address,x.services].join(' ').includes(q));return <section className="module-panel generated-module"><h1>{language==='zh'?'早療社區療育服務':'Early Intervention Services'}</h1><p className="notice subtle">{language==='zh'?'服務內容為來源記錄；請直接向單位確認實際服務、資格、時間與安排。':'Service descriptions are source records; confirm actual services, eligibility, hours, and arrangements with the organization.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區、地址或服務內容':'Search institution, district, address, or services'}/><div className="notes-grid">{f.map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.address||'—'}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p><details><summary>{language==='zh'?'服務內容':'Services'}</summary><p>{x.services||'—'}</p></details>{x.address&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a>}</article>)}</div></section>}
