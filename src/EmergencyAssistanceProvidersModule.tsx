import{useEffect,useState}from'react';export default function EmergencyAssistanceProvidersModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/emergency-assistance-providers/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'急難救助提供單位':'Emergency Assistance Providers'}</h1><p className="notice subtle">{language==='zh'?'資料為臺北市政府公開之急難救助提供單位資訊。實際服務內容、申請資格與受理時間請直接洽詢各提供單位。':'This dataset lists emergency assistance providers published by Taipei City. Contact individual organizations to confirm eligibility, services, and operating hours.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、類型或地址':'Search institution, type, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.institutionType,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.institutionType} · {x.district||'—'}</p><p>{x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
