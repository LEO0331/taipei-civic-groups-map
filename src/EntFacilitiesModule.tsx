import{useEffect,useMemo,useState}from'react';export default function EntFacilitiesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/ent-facilities/records.json`).then(x=>x.json()).then(setR)},[]);const f=useMemo(()=>r.filter(x=>!q||[x.name,x.district,x.address,x.phone,'耳鼻喉科 ENT'].join(' ').includes(q)),[r,q]);return <section className="module-panel generated-module"><h1>{language==='zh'?'臺北市耳鼻喉科醫療機構':'ENT Medical Facilities'}</h1><p className="notice subtle">{language==='zh'?'醫療機構資訊來自臺北市公開資料；請直接向院所確認目前門診時間與可提供服務。':'Medical facility information is based on Taipei City open data. Please confirm current clinic hours and available services directly with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區、地址或電話':'Search facility, district, address, or phone'}/><div className="summary-grid"><article><span>{language==='zh'?'耳鼻喉科院所':'ENT facilities'}</span><strong>{f.length}</strong></article></div><div className="notes-grid">{f.map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.address||'—'}</p><p>{x.phone?<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>:'—'}</p>{x.address&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Map lookup'}</a>}</article>)}</div></section>}
