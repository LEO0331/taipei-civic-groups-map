import{useEffect,useState}from'react';export default function EntrustedPublicAssetOperationsModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/entrusted-public-asset-operations/records.json`).then(x=>x.json()).then(setR)},[]);const f=r.filter(x=>!q||[x.caseName,x.agencyOriginal,x.operatorOriginal].join(' ').includes(q)),ops=new Set(f.map(x=>x.operatorOriginal).filter(Boolean)),agencies=new Set(f.map(x=>x.agencyCode||x.agencyOriginal).filter(Boolean));return <section className="module-panel generated-module"><h1>{language==='zh'?'市有財產委託經營':'Entrusted Public Asset Operations'}</h1><p className="notice subtle">{language==='zh'?'期間與屆期判讀僅依資料所載委託經營期間，不代表即時契約狀態或重新招標。':'Period interpretations are based only on recorded operating periods, not live contract status or future tender action.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋案件、辦理機關或受託單位':'Search case, agency, or operator'}/><div className="summary-grid"><S l={language==='zh'?'案件數':'Cases'} n={f.length}/><S l={language==='zh'?'辦理機關':'Managing agencies'} n={agencies.size}/><S l={language==='zh'?'受託單位':'Operators'} n={ops.size}/></div><div className="comparison-scroll"><table className="procurement-table"><thead><tr><th>{language==='zh'?'案件名稱':'Case'}</th><th>{language==='zh'?'辦理機關':'Agency'}</th><th>{language==='zh'?'受託單位':'Operator'}</th><th>{language==='zh'?'委託經營期間':'Recorded period'}</th></tr></thead><tbody>{f.map(x=><tr key={x.id}><th>{x.caseName}</th><td>{x.agencyOriginal}</td><td>{x.operatorOriginal}</td><td>{x.periodOriginal}</td></tr>)}</tbody></table></div></section>}function S({l,n}:{l:string;n:number}){return <article><span>{l}</span><strong>{n}</strong></article>}
