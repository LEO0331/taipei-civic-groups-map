import{useEffect,useState}from'react';export default function GbsScreeningClinicsModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/gbs-screening-clinics/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'孕婦 GBS 篩檢特約院所':'GBS Screening Clinics'}</h1><p className="notice subtle">{language==='zh'?'本資料為孕婦乙型鏈球菌篩檢補助特約院所名單；實際服務、資格與受理安排請直接向院所確認。':'This is a directory of contracted institutions for subsidized GBS screening for pregnant women. Confirm services, eligibility, and arrangements directly with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋院所、行政區或地址':'Search clinic, district, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.district,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.address}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
