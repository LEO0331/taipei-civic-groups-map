import{useMemo,useState}from'react';export default function GeneratedDatasetDirectoryModule({title,subtitle,records,language,columns,notice}:{title:string;subtitle:string;records:any[];language:'zh'|'en';columns:Array<[string,string]>;notice:string}){const[search,setSearch]=useState(''),r=useMemo(()=>records.filter(x=>JSON.stringify(x).toLowerCase().includes(search.toLowerCase())),[records,search]);return <section className="workspace"><div className="section-heading"><p>PUBLIC RECORD DIRECTORY</p><h2>{title}</h2><span>{subtitle}</span></div><div className="notice subtle">{notice}</div><label className="search"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={language==='zh'?'搜尋資料':'Search records'}/></label><div className="section-heading inline"><p>{language==='zh'?'篩選後紀錄':'Filtered records'}</p><strong>{r.length}</strong></div><div className="comparison-scroll procurement-table"><table><thead><tr>{columns.map(([,label])=><th key={label}>{label}</th>)}</tr></thead><tbody>{r.map((x,i)=><tr key={x.id??i}>{columns.map(([key])=><td key={key}>{Array.isArray(x[key])?x[key].join('、'):x[key]??'-'}</td>)}</tr>)}</tbody></table></div></section>}
