import{useEffect,useState}from'react';const fmt=(n:number)=>`NT$${n.toLocaleString()}`;export default function HealthcareWelfareBudgetModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/healthcare-welfare-budget/records.json`).then(x=>x.json()).then(setR)},[]);const f=r.filter(x=>!q||x.name.includes(q)),total=f.reduce((n,x)=>n+(x.totalBudget??0),0),original=f.reduce((n,x)=>n+(x.originalBudget??0),0),change=f.reduce((n,x)=>n+(x.budgetChange??0),0);return <section className="module-panel generated-module"><h1>{language==='zh'?'醫療保健福利業務預算':'Healthcare & Welfare Programs Budget'}</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋業務名稱':'Search program'}/><div className="summary-grid"><S l={language==='zh'?'業務項目':'Programs'} n={f.length}/><S l={language==='zh'?'原預算總額':'Original budget'} n={original}/><S l={language==='zh'?'預算增減':'Budget change'} n={change}/><S l={language==='zh'?'調整後預算':'Revised budget'} n={total}/></div><div className="comparison-scroll"><table className="procurement-table"><thead><tr><th>{language==='zh'?'名稱':'Program'}</th><th>{language==='zh'?'原預算':'Original'}</th><th>{language==='zh'?'增減':'Change'}</th><th>{language==='zh'?'合計':'Total'}</th></tr></thead><tbody>{f.sort((a,b)=>(b.totalBudget??0)-(a.totalBudget??0)).map(x=><tr key={x.id}><th>{x.name}</th><td>{x.originalBudget==null?'—':fmt(x.originalBudget)}</td><td>{x.budgetChange==null?'—':fmt(x.budgetChange)}</td><td>{x.totalBudget==null?'—':fmt(x.totalBudget)}</td></tr>)}</tbody></table></div></section>}function S({l,n}:{l:string;n:number}){return <article><span>{l}</span><strong>{typeof n==='number'?fmt(n):n}</strong></article>}
