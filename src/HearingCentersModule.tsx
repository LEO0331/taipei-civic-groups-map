import{useEffect,useState}from'react';export default function HearingCentersModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/hearing-centers/records.json`).then(x=>x.json()).then(setR)},[]);const f=r.filter(x=>!q||[x.name,x.district,x.address,x.postalCode].join(' ').includes(q));return <section className="module-panel generated-module"><h1>{language==='zh'?'臺北市聽力所':'Taipei Hearing Centers'}</h1><p className="notice subtle">{language==='zh'?'來源未提供官方座標，僅提供外部地址查詢。':'The source has no official coordinates; external address lookup only.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋名稱、行政區、郵遞區號或地址':'Search name, district, postal code, or address'}/><div className="notes-grid">{f.map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.postalCode||'—'}</p><p>{x.address||'—'}</p>{x.address&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a>}</article>)}</div></section>}
