import{useEffect,useState}from'react';export default function HighMyopiaPreventionClinicsModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/high-myopia-prevention-clinics/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'學童高度近視防治眼科合約院所':'Schoolchildren High-Myopia Prevention Contracted Clinics'}</h1><p className="notice subtle">{language==='zh'?'本名單僅代表參與此防治專案的合約院所，不是臺北市全部眼科清單；門診與服務請先向院所確認。':'This is a list of clinics participating in this prevention program, not all ophthalmology providers in Taipei. Confirm clinics and services with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋院所、行政區、地址或電話':'Search clinic, district, address, or phone'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.district,x.address,x.phone].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district} · {x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p>{x.otherContact&&<p>{x.otherContact}</p>}<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
