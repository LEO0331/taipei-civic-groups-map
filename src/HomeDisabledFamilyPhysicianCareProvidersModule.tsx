import{useEffect,useMemo,useState}from'react';export default function HomeDisabledFamilyPhysicianCareProvidersModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/home-disabled-family-physician-care-providers/records.json`).then(x=>x.json()).then(setR)},[]);const f=useMemo(()=>r.filter(x=>!q||[x.institutionName,x.serviceAreaRaw,x.districtName,x.institutionAddress,x.phoneRaw,x.mobileRaw].join(' ').includes(q)),[r,q]);return <section className="module-panel generated-module"><h1>{language==='zh'?'居家失能個案家庭醫師照護服務特約單位':'Home-Based Family Physician Care Providers'}</h1><p className="notice subtle">{language==='zh'?'本名冊不表示目前特約、收案、到府、醫師指派、資格或預約可用性。':'This directory does not establish current contracting, case acceptance, home visits, physician assignment, eligibility, or appointments.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、服務區域、地址或電話':'Search provider, service area, address, or phone'}/><div className="summary-grid"><article><span>{language==='zh'?'來源記錄':'Source records'}</span><strong>{f.length}</strong></article></div><div className="notes-grid">{f.map(x=><article key={x.id}><h2>{x.institutionName}</h2><p>{language==='zh'?'服務區域':'Service area'}: {x.serviceAreaRaw||'—'}</p><p>{x.institutionAddress||'—'}</p><p>{x.phoneRaw||x.mobileRaw||'—'}</p>{x.institutionAddress&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Map lookup'}</a>}</article>)}</div></section>}
