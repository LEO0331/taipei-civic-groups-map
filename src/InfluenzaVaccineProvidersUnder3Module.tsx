import{useEffect,useState}from'react';export default function InfluenzaVaccineProvidersUnder3Module({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/influenza-vaccine-providers-under3/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'3歲以下幼兒流感疫苗合約院所':'Flu Vaccination Clinics – Children Under 3'}</h1><p className="notice subtle">{language==='zh'?'疫苗庫存、接種資格及門診時間可能變動，前往前建議先致電院所確認。':'Vaccine stock, eligibility, and clinic hours may change. Contact the facility before visiting.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋院所、行政區或地址':'Search clinic, district, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.institutionName,x.districtName,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.institutionName}</h2><p>{x.districtName} · {x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
