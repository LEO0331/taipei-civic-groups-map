import{useEffect,useState}from'react';import GeneratedDatasetDirectoryModule from'./GeneratedDatasetDirectoryModule';export default function InternalMedicineInstitutionsModule({language}:{language:'zh'|'en'}){const[records,setRecords]=useState<any[]>([]);useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/internal-medicine-institutions/records.json`).then(r=>r.json()).then(setRecords).catch(()=>setRecords([]))},[]);return <GeneratedDatasetDirectoryModule language={language} records={records} title="Taipei Internal Medicine Institutions" subtitle={language==='zh'?'內科醫療機構公開名冊；依名稱、行政區、郵遞區號與來源聯絡資訊查詢。':'Source-recorded internal medicine institution directory, searchable by name, district, postal code, and source contact information.'} columns={[['sourceSequenceNumber',language==='zh'?'序號':'ID'],['institutionName',language==='zh'?'機構名稱':'Institution'],['districtName',language==='zh'?'行政區（衍生）':'District (derived)'],['postalCode',language==='zh'?'郵遞區號':'Postal code'],['address',language==='zh'?'地址':'Address'],['phoneRaw',language==='zh'?'電話':'Telephone'],['externalMapQuery',language==='zh'?'地圖查詢':'Map lookup']]} notice={language==='zh'?'本名冊反映資料更新時的內科機構紀錄，不表示目前門診時間、醫師出勤、預約狀態、次專科或特定診療服務。請直接向機構確認。':'This directory reflects institution records at the source update time and does not establish current clinic hours, physician attendance, appointment availability, subspecialties, or particular services. Confirm directly with the institution.'}/>}
