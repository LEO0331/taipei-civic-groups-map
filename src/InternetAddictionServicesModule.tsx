import{useEffect,useState}from'react';export default function InternetAddictionServicesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/internet-addiction-services/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'網癮治療服務機構':'Internet Addiction Services'}</h1><p className="notice subtle">{language==='zh'?'本頁資訊整理自臺北市政府開放資料，實際服務項目、門診時間及資格請以各機構最新公告為準。':'Information is based on Taipei City open data. Please confirm current services, appointment availability, and eligibility directly with the provider.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區、地址或服務類型':'Search institution, district, address, or service type'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.district,x.address,x.serviceTypeRaw].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district} · {x.serviceTypeRaw}</p><p>{x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p>{x.website&&<p><a href={x.website} target="_blank" rel="noopener noreferrer">{language==='zh'?'官方網站':'Official website'}</a></p>}<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
