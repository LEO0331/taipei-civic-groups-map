import{useEffect,useState}from'react';export default function KidneyHealthPromotionFacilitiesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/kidney-health-promotion-facilities/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'腎臟病健康促進機構':'Kidney Health Promotion Facilities'}</h1><p className="notice subtle">{language==='zh'?'本名單反映來源列出的健康促進機構，不代表即時門診、服務或資格。':'This directory reflects source-listed health-promotion facilities and does not establish real-time clinics, services, or eligibility.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋院所、行政區、郵遞區號或地址':'Search facility, district, postal code, or address'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.district,x.postalCode,x.address].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.postalCode||'—'}</p><p>{x.address}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
