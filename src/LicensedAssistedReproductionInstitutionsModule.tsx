import SourceFields from './SourceFields';
import{useEffect,useMemo,useState}from'react';type R={id:string;cityName:string;institutionName:string;nhiContractedRaw:string;nhiContractedStatus:string;note:string;hasNote:boolean;sourceValues:Record<string,string>};export default function LicensedAssistedReproductionInstitutionsModule({language}:{language:'zh'|'en'}){const zh=language==='zh',t=(z:string,e:string)=>zh?z:e,[r,setR]=useState<R[]>([]),[q,setQ]=useState(''),[city,setCity]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/licensed-assisted-reproduction-institutions/records.json`).then(x=>x.json()).then(setR)},[]);const f=useMemo(()=>r.filter(x=>(!q||[x.cityName,x.institutionName,x.nhiContractedRaw,x.note].join(' ').toLowerCase().includes(q.toLowerCase()))&&(!city||x.cityName===city)),[r,q,city]);return <section className="workspace"><div className="section-heading"><p>HEALTHCARE PROVIDERS / REPRODUCTIVE HEALTH / ASSISTED REPRODUCTION</p><h2>{t('特約人工生殖機構名單','Licensed Assisted Reproduction Institutions')}</h2><span>{t('來源登錄的行政名錄；不提供地址、地圖、即時名額、療程或補助判定。','Source-recorded administrative directory; no addresses, maps, real-time capacity, treatment, or subsidy determinations.')}</span></div><div className="notice subtle">{t('本資料不確認目前許可、營運、預約、療程、容量、補助資格、健保給付、費用、成功率、品質、安全或推薦，也不會收集或推定您的生育、懷孕或健康資訊。','This dataset does not confirm current permission, operation, appointments, treatment, capacity, subsidy eligibility, NHI coverage, fees, success rates, quality, safety, or recommendation. It does not collect or infer fertility, pregnancy, or health information.')}</div><aside className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder={t('搜尋機構、縣市、特約狀態或備註','Search institution, city, contracted status, or note')}/><select value={city} onChange={e=>setCity(e.target.value)}><option value="">{t('全部縣市','All cities')}</option>{[...new Set(r.map(x=>x.cityName).filter(Boolean))].map(x=><option key={x}>{x}</option>)}</select></aside><div className="summary-grid">{[[t('來源紀錄','Source records'),f.length],[t('不重複機構','Unique institutions'),new Set(f.map(x=>x.institutionName)).size],[t('特約：是','Source-recorded yes'),f.filter(x=>x.nhiContractedStatus==='yes').length],[t('未知或條件','Unknown or conditional'),f.filter(x=>x.nhiContractedStatus==='unknown'||x.nhiContractedStatus==='conditional').length],[t('有備註','With notes'),f.filter(x=>x.hasNote).length]].map(([l,x])=><article key={String(l)}><span>{l}</span><strong>{x}</strong></article>)}</div><div className="comparison-scroll procurement-table"><table><thead><tr>{[t('縣市','City'),t('人工生殖機構','Assisted reproduction institution'),t('健保特約機構（來源）','NHI contracted institution (source)'),t('備註','Notes')].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{f.map(x=><tr key={x.id}><td>{x.cityName||'—'}</td><th>{x.institutionName||'—'}</th><td>{x.nhiContractedRaw||'—'}</td><td>{x.note||'—'}<details><summary>{t('來源欄位','Source fields')}</summary><SourceFields fields={x.sourceValues} /></details></td></tr>)}</tbody></table></div></section>}
