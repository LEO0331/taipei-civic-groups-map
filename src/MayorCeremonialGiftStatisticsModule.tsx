import{useEffect,useState}from'react';export default function MayorCeremonialGiftStatisticsModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[y,setY]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/mayor-ceremonial-gift-statistics/records.json`).then(x=>x.json()).then(setR)},[]);const f=r.filter(x=>!y||String(x.year)===y),total=f.reduce((n,x)=>n+x.total,0);return <section className="module-panel generated-module"><h1>{language==='zh'?'市長喜喪致贈統計':'Mayor Ceremonial Gift Statistics'}</h1><p className="notice subtle">{language==='zh'?'本資料為月度統計，僅呈現來源所載件數與時間變化，不推論收件人或原因。':'This is monthly source statistics; it presents recorded counts and changes only, without inferring recipients or causes.'}</p><select value={y} onChange={e=>setY(e.target.value)}><option value="">{language==='zh'?'全部年度':'All years'}</option>{[...new Set(r.map(x=>x.year))].map(x=><option key={x}>{x}</option>)}</select><div className="summary-grid"><article><span>{language==='zh'?'總致贈件數':'Total items'}</span><strong>{total}</strong></article><article><span>{language==='zh'?'電子輓聯':'Electronic elegiac couplets'}</span><strong>{f.reduce((n,x)=>n+(x.elegiac??0),0)}</strong></article></div><div className="comparison-scroll"><table className="procurement-table"><thead><tr><th>{language==='zh'?'年度':'Year'}</th><th>{language==='zh'?'月份':'Month'}</th><th>{language==='zh'?'總計':'Total'}</th></tr></thead><tbody>{f.map(x=><tr key={x.id}><td>{x.year}</td><td>{x.month}</td><td>{x.total}</td></tr>)}</tbody></table></div></section>}
