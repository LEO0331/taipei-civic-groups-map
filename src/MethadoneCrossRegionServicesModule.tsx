import{useEffect,useState}from'react';export default function MethadoneCrossRegionServicesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/methadone-cross-region-services/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'美沙冬跨區給藥服務':'Methadone Cross-Region Service'}</h1><p className="notice subtle">{language==='zh'?'服務時間、費用及服務方式可能調整，實際資訊請以服務機構及主管機關最新公告為準。前往前建議先致電確認。':'Service hours, fees, and procedures may change. Please confirm the latest information with the institution or relevant authority before visiting.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、地址或注意事項':'Search institution, address, or notes'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.address,x.notes].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}{x.extension&&` #${x.extension}`}</a>}</p><details><summary>{language==='zh'?'官方公告給藥時間與費用':'Source service hours and fees'}</summary><p>{language==='zh'?'平日':'Weekday'}: {x.weekdayHours||'—'}</p><p>{language==='zh'?'假日':'Holiday'}: {x.holidayHours||'—'}</p><p>{language==='zh'?'跨區給藥服務費':'Service fee'}: {x.serviceFee||'未提供'}</p><p>{language==='zh'?'注意事項':'Notes'}: {x.notes||'—'}</p></details><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
