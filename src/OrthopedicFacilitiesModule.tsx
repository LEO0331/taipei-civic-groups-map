import{useEffect,useMemo,useState}from'react';export default function OrthopedicFacilitiesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState(''),[d,setD]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/orthopedic-facilities/records.json`).then(x=>x.json()).then(setR)},[]);const f=useMemo(()=>r.filter(x=>(!q||[x.name,x.district,x.postalCode,x.address].join(' ').includes(q))&&(!d||x.district===d)),[r,q,d]);return <section className="module-panel generated-module"><h1>{language==='zh'?'臺北市骨科醫療機構':'Orthopedic Medical Facilities'}</h1><p className="notice subtle">{language==='zh'?'來源未提供官方座標；本目錄僅提供外部地址查詢，不表示目前門診、治療或預約可用性。':'The source has no official coordinates; this directory provides external address lookup only and does not establish current clinics, treatment, or appointment availability.'}</p><div className="filters filter-grid"><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區、郵遞區號或地址':'Search facility, district, postal code, or address'}/><select value={d} onChange={e=>setD(e.target.value)}><option value="">{language==='zh'?'全部行政區':'All districts'}</option>{[...new Set(r.map(x=>x.district).filter(Boolean))].map(x=><option key={x}>{x}</option>)}</select></div><div className="summary-grid"><article><span>{language==='zh'?'骨科醫療機構':'Orthopedic facilities'}</span><strong>{f.length}</strong></article><article><span>{language==='zh'?'涵蓋行政區':'Districts covered'}</span><strong>{new Set(f.map(x=>x.district).filter(Boolean)).size}</strong></article></div><div className="notes-grid">{f.map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.district||'—'} · {x.postalCode||'—'}</p><p>{x.address||'—'}</p>{x.address&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Map lookup'}</a>}</article>)}</div></section>}
