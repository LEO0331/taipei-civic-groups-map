import SourceFields from './SourceFields';
import{useEffect,useMemo,useState}from'react';export default function RehabilitationMedicineInstitutionsModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState(''),[d,setD]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/rehabilitation-medicine-institutions/records.json`).then(x=>x.json()).then(setR)},[]);const f=useMemo(()=>r.filter(x=>(!q||[x.institutionName,x.districtName,x.districtCode,x.address].join(' ').includes(q))&&(!d||x.districtName===d)),[r,q,d]);return <section className="module-panel generated-module"><h1>{language==='zh'?'臺北市復健科醫療機構':'Taipei Rehabilitation Medicine Institutions'}</h1><p className="notice subtle">{language==='zh'?'本名冊不表示目前門診、復健治療、醫師、設備或預約可用性。':'This listing does not establish current clinic, rehabilitation-treatment, physician, equipment, or appointment availability.'}</p><div className="filters filter-grid"><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、行政區、代碼或地址':'Search institution, district, code, or address'}/><select value={d} onChange={e=>setD(e.target.value)}><option value="">{language==='zh'?'全部行政區':'All districts'}</option>{[...new Set(r.map(x=>x.districtName).filter(Boolean))].map(x=><option key={x}>{x}</option>)}</select></div><div className="summary-grid"><article><span>{language==='zh'?'來源記錄':'Source records'}</span><strong>{f.length}</strong></article><article><span>{language==='zh'?'行政區':'Districts'}</span><strong>{new Set(f.map(x=>x.districtName).filter(Boolean)).size}</strong></article></div><div className="notes-grid">{f.map(x=><article key={x.id}><h2>{x.institutionName}</h2><p>{x.districtName||'—'} · {x.districtCode||'—'}</p><p>{x.address||'—'}</p>{x.address&&<a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Map lookup'}</a>}<details><summary>{language==='zh'?'來源細節':'Source details'}</summary><SourceFields fields={x.sourceValues} /></details></article>)}</div></section>}
