import{useEffect,useState}from'react';export default function SeniorServicesModule({language}:{language:'zh'|'en'}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/senior-services/records.json`).then(x=>x.json()).then(setR)},[]);return <section className="module-panel generated-module"><h1>{language==='zh'?'銀髮服務機構':'Senior Services'}</h1><p className="notice subtle">{language==='zh'?'所在地與資料集收錄不等同服務範圍；請向機構確認實際服務與安排。':'Physical location and dataset membership do not establish service coverage; confirm actual services and arrangements with the institution.'}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={language==='zh'?'搜尋機構、類型、行政區、地址或電話':'Search institution, type, district, address, or phone'}/><div className="notes-grid">{r.filter(x=>!q||[x.name,x.categoryRaw,x.district,x.address,x.phone].join(' ').includes(q)).map(x=><article key={x.id}><h2>{x.name}</h2><p>{x.categoryRaw} · {x.locationCity}</p><p>{x.district||'—'} · {x.address}</p><p>{x.phone&&<a href={`tel:${x.phone.replace(/[^+\d]/g,'')}`}>{x.phone}</a>}</p><p>{x.establishedDate}</p><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.externalMapQuery)}`}>{language==='zh'?'外部地圖查詢':'Directions'}</a></article>)}</div></section>}
