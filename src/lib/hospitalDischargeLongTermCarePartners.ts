export type HospitalDischargeLtcPartnerRecord = { id: string; districtRaw: string; districtName: string; organizationName: string; primaryContactName: string; phoneRaw: string; phone: string; extensionRaw: string; extension: string; mobileRaw: string; mobile: string; locationRaw: string; address: string | null; longitude: number | null; latitude: number | null; locationType: 'address' | 'wgs84_coordinates' | 'twd97_coordinates' | 'other' | 'unknown'; hasPhone: boolean; hasMobile: boolean; hasValidLocation: boolean; externalMapQuery: string | null; sourceValues: Record<string, string> };
export const cleanDischargeLtcValue = (value?: string) => (value ?? '').replace(/\s+/g, ' ').trim();
export const normalizeDischargeLtcText = (value?: string) => cleanDischargeLtcValue(value).replace(/臺/g, '台').toLocaleLowerCase();
export const fullDischargePhone = (phone: string, extension: string, language: 'zh' | 'en') => extension ? `${phone}${language === 'zh' ? ' 分機 ' : ' ext. '}${extension.replace(/^#/, '')}` : phone;
